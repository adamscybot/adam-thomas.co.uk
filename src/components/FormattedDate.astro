---
interface Props {
  date: Date
  options?: Intl.DateTimeFormatOptions
}

const { date, options = { year: '2-digit', month: 'short' } } = Astro.props
---

<time datetime={date.toISOString()}>
  {date.toLocaleDateString('en-gb', options)}
</time>
